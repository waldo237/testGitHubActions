
name: update-master-from-migration

on: 
  push:
    branches: "feature/lightspeed-transition"
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - id: automerge
        name: automerge
        uses: "pascalgn/automerge-action@v0.15.3"
